// Globally ignored CSS selectors — applied across modules that respect
// window.__ignoreSelectors (currently links + validation). Users can disable
// individual entries or add their own via Settings.
export const BUILT_IN_IGNORE_SELECTORS = [
  '.WidgetSealContainer',
]
